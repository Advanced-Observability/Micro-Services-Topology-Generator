  ${name}:
    name: ${name}
    enable_ipv6: true
    driver: bridge
    driver_opts:
      com.docker.network.enable_ipv6: "true"
    ipam:
      driver: default
      config:
       - subnet: ${subnet}
         gateway: ${gateway}
