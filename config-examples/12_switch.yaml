frontend:
  type: service
  port: 80
  endpoints:
    - entrypoint: /
      respsize: 512
      connections:
        - path: s1->db
          url: /
s1:
  type: switch
  neighbors:
    - hop: frontend
    - hop: db
db:
  type: service
  port: 10001
  endpoints:
    - entrypoint: /
      respsize: 128
