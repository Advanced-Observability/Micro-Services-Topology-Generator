frontend:
  type: external
  image: myimage
  ports:
    - 80
  connections:
    - path: r1->db
r1:
  type: router
  neighbors:
    - db
db:
  type: service
  port: 10001
  endpoints:
    - entrypoint: /
      respsize: 128
